networkx>=3.2
numpy>=1.26
pandas>=2.2
matplotlib>=3.8
plotly>=5.22
PyYAML>=6.0
psutil>=5.9
scikit-learn>=1.4
# Deep Reinforcement Learning stack (Farahi et al. 2026 alignment)
torch>=2.0.0
torchvision>=0.15.0
torch-geometric>=2.4.0
# Simulation and network analysis
# mininet>=2.3.0
# Mininet is provided by OS packages on most Linux distributions and
# is not reliably available via pip. Install system Mininet instead:
#   sudo apt-get update && sudo apt-get install -y mininet openvswitch-switch iperf3
# If you intentionally want a pip-installable Mininet variant, uncomment
# the line above and accept the risk of incompatibility on some Python versions.
ipython>=8.0.0
